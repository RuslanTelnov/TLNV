# 🛠️ Troubleshooting & Knowledge Base

> [!IMPORTANT]
> **ПРОТОКОЛ:** Все возникающие в ходе работы ошибки и найденные решения должны быть задокументированы в этой папке.

Здесь собраны решения частых проблем и ошибок, возникающих при работе с проектом.

## 📂 Содержание

### 1. [Парсинг Wildberries](./wb_parsing.md)
- Почему нет фото у товаров? (`basket-XX` issue)
- Проблемы с Selenium (модули, селекторы).

### 2. [Интеграции (MoySklad, Kaspi)](./integration_errors.md)
- Ошибки скриптов Python при вызове из Node.js (`Invalid script output`).
- Ошибки API Kaspi (500 Error, Content-Type).
- Проблемы с API Routes в Next.js.

### 3. [Фронтенд (Next.js)](./frontend_errors.md)
- Ошибки гидратации (`<tr>` whitespace).

---

## 💡 Быстрый чек-лист при ошибках

1. **Python**: Проверьте версию (`python3 --version`). Установлены ли пакеты (`pip list`)?
2. **Переменные окружения**: Проверьте `.env` и `.env.local`. Существуют ли ключи?
3. **Логи**: Смотрите вывод в терминале, где запущен `npm run dev` или Python-скрипт.
4. **JSON обертка**: Если Python вызывается из JS — всегда проверяйте наличие `JSON_START`...`JSON_END`.
